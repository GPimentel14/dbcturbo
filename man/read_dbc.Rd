% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbcturbo.R
\name{read_dbc}
\alias{read_dbc}
\title{Read a DATASUS DBC file into an R data frame}
\usage{
read_dbc(file, batch_size = 4096L, encoding = "CP850", verbose = FALSE, ...)
}
\arguments{
\item{file}{Character string. Path to the \code{.dbc} file.}

\item{batch_size}{Integer. Passed to \code{\link{dbc_to_csv}}.
Default \code{4096L}.}

\item{encoding}{Character string. Source encoding of character fields.
Default \code{"CP850"} (standard DATASUS legacy encoding).}

\item{verbose}{Logical. Passed to \code{\link{dbc_to_csv}}.
Default \code{FALSE} for in-memory reading.}

\item{...}{Additional arguments forwarded to the CSV reader.}
}
\value{
A \code{data.frame} or \code{data.table} (if \pkg{data.table}
  is installed).
}
\description{
High-level convenience wrapper that streams the \code{.dbc} file to a
temporary CSV via \code{\link{dbc_to_csv}} and then reads it with
\code{data.table::fread()} (if available) or \code{utils::read.csv()}.
}
\details{
For files with more than one million records, use \code{\link{dbc_to_csv}}
directly and load the resulting CSV with \code{arrow::read_csv_arrow()} or
\code{data.table::fread()} for maximum performance.
}
\examples{
\dontrun{
df <- read_dbc("SINAN_DENGUE_RS_2024.dbc")
head(df)
}

}
