## Test environments
* local Ubuntu 24.04.4 LTS, R 4.6.1
* win-builder (devel and release)
* R-hub builder

## R CMD check results
There were 0 ERRORs, 0 WARNINGs, 0 NOTEs.

## Initial submission
* This is a new release of `dbcturbo`.
* The package provides a high-performance streaming reader and converter for Brazilian Ministry of Health DATASUS .dbc files.
* PKWare Implode decompression algorithm is based on `blast.c` by Mark Adler (properly credited in `Authors@R` and source code).
