% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbcturbo.R
\name{dbc_to_csv}
\alias{dbc_to_csv}
\title{Convert a DATASUS DBC file directly to CSV (streaming, low-RAM)}
\usage{
dbc_to_csv(
  input_file,
  output_file,
  batch_size = 4096L,
  encoding = NULL,
  progress = NULL
)
}
\arguments{
\item{input_file}{Character string. Path to the source \code{.dbc} file.}

\item{output_file}{Character string. Path for the output \code{.csv} file.}

\item{batch_size}{Integer \code{>= 1}. Number of records per write batch.
Default \code{4096L}.}

\item{encoding}{Character string or \code{NULL}. Source encoding of
character fields (e.g. \code{"CP850"}, \code{"latin1"}).
\code{NULL} (default) assumes ASCII / UTF-8.}

\item{progress}{Function or \code{NULL}. Called after each batch with
two numeric arguments: \code{done} and \code{total} (record counts).
Ctrl+C is honoured between batches.}
}
\value{
\code{TRUE} invisibly on success. On failure, stops with a
  descriptive error message.
}
\description{
Decompresses a \code{.dbc} file and writes a UTF-8 CSV to disk.
Records are processed in batches, so peak RAM usage is
\code{O(batch_size * record_width)} regardless of file size.
}
\details{
The output CSV is prefixed with a UTF-8 BOM so that Microsoft Excel
opens it with correct encoding without additional configuration.
}
\examples{
\dontrun{
# Simple usage: just provide input and output paths
dbc_to_csv("SINASC_RS_2024.dbc", "SINASC_RS_2024.csv")

# Advanced usage: with progress bar and custom batch size
dbc_to_csv(
  input_file  = "SINASC_RS_2024.dbc",
  output_file = "SINASC_RS_2024.csv",
  batch_size  = 10000L,
  progress    = function(done, total)
    cat(sprintf("\r  \%.1f\%\%", 100 * done / total))
)
library(data.table)
dt <- fread("SINASC_RS_2024.csv")
}

}
\seealso{
\code{\link{dbc2dbf}}, \code{\link{dbc_inspect}}
}
