% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbcturbo.R
\name{dbc_batch_to_csv}
\alias{dbc_batch_to_csv}
\title{Convert all DBC files in a directory to CSV files}
\usage{
dbc_batch_to_csv(
  input_dir,
  output_dir,
  pattern = "\\\\.dbc$",
  recursive = FALSE,
  batch_size = 4096L,
  encoding = NULL,
  overwrite = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{input_dir}{Character string. Directory containing DBC files.}

\item{output_dir}{Character string. Destination directory for CSV files.
Created when it does not exist.}

\item{pattern}{Regular expression used to select input files. Defaults to
\code{"\\\\.dbc$"}, case-insensitively.}

\item{recursive}{Logical. Search subdirectories? Default \code{FALSE}.}

\item{batch_size}{Integer. Number of records per CSV write batch. Passed to
\code{\link{dbc_to_csv}}.}

\item{encoding}{Character string or \code{NULL}. Passed to
\code{\link{dbc_to_csv}}.}

\item{overwrite}{Logical. Replace existing CSV files? Default \code{FALSE}.}

\item{verbose}{Logical. Print per-file conversion progress? Default
\code{FALSE}.}
}
\value{
A data frame with one row per converted file and columns
  \code{input_file} and \code{output_file}. For an empty input directory,
  returns a zero-row data frame with those columns.
}
\description{
Finds DBC files in a directory and converts each one with
\code{\link{dbc_to_csv}}. When \code{recursive = TRUE}, the relative
directory layout beneath \code{input_dir} is preserved in
\code{output_dir}. Existing output files are never replaced unless
\code{overwrite = TRUE}.
}
\examples{
\dontrun{
result <- dbc_batch_to_csv("data/dbc", "data/csv", recursive = TRUE)
}
}
